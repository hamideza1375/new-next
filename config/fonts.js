import {
    Vazirmatn,
    Shantell_Sans,
} from 'next/font/google';

export const fontVazir = Vazirmatn({
    subsets: ['arabic'],
    variable: '--font-vazirmatn'
});

export const fontSans = Shantell_Sans({
    subsets: ['latin'],
    variable: '--font-tanha'
});
